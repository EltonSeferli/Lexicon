export function authPage(mode) {
  const isRegister = mode === "register";
  return `<main class="auth-shell"><section class="auth-panel"><div class="auth-brand"><img src="/logo.svg" alt="Lexicon logo"><span>Lexicon</span></div><p class="auth-eyebrow">PERSONAL STUDY SPACE</p><h1>${isRegister ? "Build your study space." : "Welcome back."}</h1><p class="auth-copy">Track your IELTS progress and vocabulary in one private place.</p><form id="auth-form" class="auth-form">${isRegister ? `<label>Full name<input id="auth-full-name" type="text" required minlength="2" autocomplete="name" placeholder="Elton Safarli"></label>` : ""}<label>Email<input id="auth-email" type="email" required autocomplete="email" placeholder="you@example.com"></label><label>Password<span class="password-field"><input id="auth-password" type="password" required minlength="8" autocomplete="${isRegister ? "new-password" : "current-password"}"><button class="password-toggle" type="button" data-action="toggle-password" aria-label="Show password" title="Show password">◉</button></span></label>${isRegister ? `<label>Confirm password<span class="password-field"><input id="auth-confirm-password" type="password" required minlength="8" autocomplete="new-password"><button class="password-toggle" type="button" data-action="toggle-password" aria-label="Show confirm password" title="Show password">◉</button></span></label>` : ""}<button class="auth-submit" type="submit">${isRegister ? "Create account" : "Log in"}<span>→</span></button></form><p class="auth-error" id="auth-error" role="alert"></p><button class="auth-switch" data-action="auth-switch" data-mode="${isRegister ? "login" : "register"}">${isRegister ? "Already have an account? Log in" : "New here? Create an account"}</button></section></main>`;
}
